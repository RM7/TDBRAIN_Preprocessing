# requirements_venv.txt
# Use pip to install these packages in your venv environment.
# Run: pip install -r requirements_venv.txt

# Example packages (replace with your actual requirements)
certifi==2020.12.5
cycler==0.10.0
kiwisolver==1.3.1
matplotlib==3.9.4 #==3.3.4 #Version does not work
numpy==2.0.2 #==1.19.2 #Version might be to old
olefile==0.46
pandas=2.3.3 #==1.2.2 #Version does not work
pickle5==0.0.11
pillow==8.1.0
pyparsing==2.4.7
python-dateutil==2.9.0.post0 #==2.8.1 #Version conflict with pandas
pytz==2021.1 #==2021.1 #Version conflict with pandas
#readline #==8.1 #Version does not exists, and does not need to be installed on macos
scipy==1.13.1 #==1.6.0 #Version creates an error
seaborn==0.13.2 #0.11.1 #other version?
setuptools==52.0.0
sip==6.8.6 #==4.19.13 #Version does not exist
six==1.15.0
tornado==6.1
wheel==0.36.2
zstd==1.5.7.2 #==1.4.5 #Version creates an error